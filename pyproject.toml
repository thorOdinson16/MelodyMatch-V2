[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "melodymatch"
version = "2.0.0"
description = "Research-grade music genre classification using FMA Medium"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]